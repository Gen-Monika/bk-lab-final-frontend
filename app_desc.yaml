specVersion: 3
module:
  language: Node.js
  spec:
    processes:
      - name: web
        procCommand: npm start
        services:
          - name: web
            exposedType:
              name: bk/http
            targetPort: 5000
            port: 80
